<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.fapp_navi_drawer">

    <application
        android:allowBackup="true"
        android:icon="@drawable/launch_icon_squat"
        android:label="@string/app_name"
        android:roundIcon="@drawable/launch_icon_squat"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".Activities.authorization.LoginActivity"
            android:label="@string/app_name">

        </activity>

        <activity
            android:name=".Activities.authorization.SignUpActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">

        </activity>
        <activity
            android:name=".Activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.SettingsActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">

        </activity>
        <activity
            android:name=".Activities.AccountActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">

        </activity>



        <activity android:name=".Activities.workout.AddUebung"/>
        <activity android:name=".Activities.workout.UpdateUebung"/>

        <activity android:name=".Activities.Food.CaloriesAndFood"/>
        <activity android:name=".Activities.Food.FoodAddActivity" />
        <activity android:name=".Activities.Food.MainFoodActivity"/>
        <activity android:name=".Activities.Food.NewFood_Activity" />

    </application>

</manifest>